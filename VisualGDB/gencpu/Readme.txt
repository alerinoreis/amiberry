This tool generates the following files:
- cputbl.h
- cpustbl.cpp
- cpuemu.cpp

Compile it for the target system (e.g. Raspberry) then copy it there to run it, along with "table68k"

Copy the generated *.cpp files back into "src" and the cputbl.h into "src/include"